// Calculando area do circulo
// Area = π*r^2
const r = 10
const π = Math.PI

let area = π*r**2

console.log('A area do circulo é de:',area.toFixed(2), 'metros')

console.log(`Area = ${π.toFixed(2)} * ${r}^2 = ${area.toFixed(2)}`) // toFixed transforma em string